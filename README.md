## Client

This is a small console applicaiton which would send out a HTTP requests to server based on the sensor data collected, on every sensor data change



## Server
This would be a REST API server that would collect the data from the different devices, and would have a SSE to send out the device updates to the front end consumers.